<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.asm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".MyApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".add.AddMethodActivity" />
        <activity android:name=".add.AddFieldActivity" />
        <activity android:name=".add.AddClassActivity" />
        <activity android:name=".exec.ExecFieldActivity" />
        <activity android:name=".exec.ExecThisActivity" />
        <activity android:name=".exec.ExecStaticActivity" />
        <activity android:name=".NextActivity" />
        <activity android:name="com.android.asm.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>